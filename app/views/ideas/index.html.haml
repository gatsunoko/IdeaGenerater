%p#notice= notice
%h1 Ideas
= link_to '登録', new_idea_path, class: 'submit my-10'
%table
  %thead
    %tr
      %th Title
      %th{:colspan => "3"}
  %tbody
    - @ideas.each do |idea|
      %tr
        %td= idea.title
        %td= link_to 'Show', idea
        %td= link_to 'Edit', edit_idea_path(idea)
        %td= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' }

.paginate
  .page_entries_info
    = page_entries_info @ideas
  .pagination
    = paginate @ideas
